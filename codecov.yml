coverage:
  precision: 2
  round: down
  range: 70..100

ignore:
  - "internal/testutil/*"
  - "internal/benchutil/*"

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: auto
        threshold: 1%
